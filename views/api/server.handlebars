{
    "name": "{{ server.preset.serverName }}"
    "preset": "{{ server.preset.presetName }}",
    "tracks": {{server.preset.track}}",
    "cars": [{{#each server.preset.cars}}"{{this}}",{{/each}}],
    "timeofday": "{{ server.preset.timeOfDay }}",
    "hasPassword": {{ server.preset.hasPassword }},
    "hasPenalties": {{ server.preset.hasPenalties }},
    "session": {{ server.session.drivers }}
}