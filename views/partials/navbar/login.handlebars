<li>
    {{#if session.passport.user}}
        <a href="/auth/logout">
            <button type="button" class="btn btn-success btn-xs">Logout</button>
        </a>
    {{else}}
        <a href="/auth/login">&nbsp;<img class="pull-right" src="/img/icons/sits_small.png" alt="Steam Login"/></a>
    {{/if}}
</li>
